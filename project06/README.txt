Assembler.rb is the driver script for the assembler. To run it, you must be in command and
enter 'assembler.rb 'file name'' where 'file name' is the path to the file you want to be assembled.
This will generate a .hack file of the same type

Assembler.rb passes the Symbol-less tests now. Generated proper hack files for all tests.

Now passes symbol-based tests. generated proper hacks.